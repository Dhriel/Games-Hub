export interface CardProps {
    url: string;
    name: string;
    slug: string;
    rating: string | number;
    id: string;
}

export interface CaterogiesProps {
    id: string;
    name: string;
}